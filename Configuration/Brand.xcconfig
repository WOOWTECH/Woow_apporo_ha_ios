// Brand.xcconfig — originally generated by Tools/brand/rebrand-ios.sh.
// The script has a one-shot guard (grep -q 'io.robbie') and can no longer be re-run
// on this tree, so this file is now HAND-MAINTAINED. Edit here, not via the script.
// These five values are the single source of truth for the brand identity; keep any
// new hard-coded brand string out of the tree and reference the variable instead.
BRAND_ID = apporo
BRAND_APP_NAME = Apporo aiot
BRAND_HOST = aiot.apporo.ai
BRAND_URL_SCHEME = apporoaiot
BRAND_BUNDLE_BASE = aiot
